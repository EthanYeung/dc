# Secrets

No real secret belongs in this repository.

Use an external secret manager or runtime credential broker. Workers should receive the narrowest possible credential scope, preferably short-lived or delegated.

This directory documents secret names and access contracts only, never values.
