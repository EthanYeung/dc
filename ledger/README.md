# Ledger

The runtime database is the operational ledger source of truth. This directory is for exported reconciliations, accounting policy and human-readable snapshots that are safe to commit.

Never treat agent-generated revenue claims as revenue until externally verified.
